{
  "version": 2,
  "builds": [
    { "src": "api/generar-pdf.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/(.*)" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
